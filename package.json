{
  "name": "token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ token",
    "create-local": "graph create --node http://localhost:8020/ token",
    "remove-local": "graph remove --node http://localhost:8020/ token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.1",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
